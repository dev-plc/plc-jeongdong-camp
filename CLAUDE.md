# CLAUDE.md — 이 저장소에서 작업할 때

PLC 정동 가을캠프 "신앙탐험대" 현장 앱. 설계 결정은 `docs-dev/spec/DECISIONS.md`(D-001~),
배포 절차는 `docs-dev/ops/DEPLOY.md` 에 있다. 이 파일은 **매번 지켜야 하는 규칙**만 둔다.

## 🔴 파일 버전 헤더 (D-049)

운영자가 요청한 규칙이다 — **파일을 고칠 때마다 헤더의 버전·수정일·변경 이력을 함께 고친다.**

대상은 `tools/check-versions.js` 의 `FILES` 목록(`gas/*.gs`, `docs/assets/js/*.js`,
`docs/assets/css/app.css`, `tools/*.js`). 새 파일을 만들면 헤더를 달고 목록에 더한다.

```
 * Code.gs · v15 · 2026-09-26                      ← 맨 위 줄: 이름 · 버전 · 수정일
 * 변경 이력 (최근 5건 — …)
 *  v15   2026-09-26  파일 버전 표시 …             ← 새 줄을 맨 위에 넣고, 5건을 넘으면 맨 아래를 뺀다
```

**버전 번호**

- `vN` — **GAS 배포 번호**. `.gs` 를 하나라도 고치면 다음 번호(현재 배포 +1)를 붙인다.
  운영자가 Apps Script 에서 "새 버전" 을 누르면 그 번호가 된다.
- `vN.k` — 서버는 vN 그대로 두고 **앱·도구만** 고친 k번째 변경. `.gs` 에는 쓰지 않는다.
- 한 번의 배포(릴리스)에서 고친 파일은 **모두 같은 번호**를 받는다. 안 고친 파일은 그대로 둔다.
- `.gs` 는 헤더 아래 `var VERSION_X = 'vN';` 도 같이 고친다. `health` 가 이 값을 알려 준다.

고친 뒤 반드시: `node tools/check-versions.js` — 어긋나면 1 로 끝난다.

**현재 배포**: GAS **v14** (2026-09-26). 다음 `.gs` 변경은 **v15**.
배포 번호가 예상과 다르게 나왔다고 운영자가 알려 주면 이 줄과 헤더를 그 번호로 맞춘다.

## 🔴 GAS 배포를 안내할 때

v13 은 `Sheets.gs` 가 빠진 채 나갔다 — 공지 삭제만 터지고 나머지는 멀쩡해 늦게 발견되는 종류다.

1. **바뀐 `.gs` 를 전부** 표로 준다. 하나만 바뀌어도 다른 파일의 새 함수를 부르면 같이 올려야 한다.
   `git diff --stat <마지막 배포 커밋>..HEAD -- gas/` 로 확인한다.
2. 파일마다 **붙여넣은 뒤 줄 수**를 함께 준다 (`wc -l`). 운영자는 줄 수로 대조한다.
3. 배포 뒤 **`{API_BASE}?action=health`** 의 `versions` 에서 볼 값을 알려 준다.
   `null` 이 보이면 버전 표시 전의 옛 파일이다.
4. **GAS 가 먼저, `docs/` 머지가 나중.** 앱이 먼저 올라가면 없는 액션을 부른다.

## 앱(`docs/`)을 고친 뒤

```
node tools/check-versions.js      # 버전 헤더
node tools/stamp-assets.js        # ?v= 해시 (브라우저 캐시를 깬다, D-036)
node tools/build-demo.js          # docs/demo.html 은 생성물 — 손으로 고치지 않는다
```

`docs/demo.html` 을 다시 만들었으면 **아티팩트 데모도 다시 올린다**
(`https://claude.ai/artifact/DQQvR4awqr3iAXfcYZHdTM`). 안 올리면 3주 동안 첫 버전이
남아 있던 일이 반복된다 (D-046). 공유 버전 이동은 운영자가 Share 메뉴에서 한다.

## 지켜 온 원칙

- **추측보다 측정.** 성능·원인은 재서 말한다. 추측이 세 번 틀렸고 잰 것만 맞았다 (D-043~045).
- **테스트는 되돌려서 무는지 본다.** 고친 줄을 빼면 깨지는 테스트여야 한다.
- 🔴 **`sb_secret_` · `service_role` 키는 `config.js` 에 절대 넣지 않는다** — 저장소가 공개다.
  명단·연락처도 커밋하지 않는다.
